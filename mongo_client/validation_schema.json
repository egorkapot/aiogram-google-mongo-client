{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["_id", "username", "role", "email", "status", "state"],
        "properties": {
            "_id": {
                "bsonType": "int"
            },
            "username": {
                "bsonType": "string"
            },
            "role": {
                "bsonType": "string",
                "enum": ["admin", "user", "biggiko", null]
            }
        }
    }
}
